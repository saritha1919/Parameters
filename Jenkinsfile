properties([parameters([booleanParam(defaultValue: false, description: '', name: 'Sonar'), booleanParam(defaultValue: false, description: '', name: 'Deploy')])])
pipeline {
	
	agent any
	stages {
		stage('Source') { 
			steps {
		              echo "display selected values:${params.Sonar}"
			 }
		 }
   }
}
